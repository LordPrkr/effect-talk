# "One Month of Effect"

> "Building with the Standard Library that TypeScript Deserves"

## Run the Presentation

```
presenterm --image-protocol kitty-remote talk.md
```

This works on Ghostty, but the images don't render while I'm in a tmux session. Exiting the session allows image to render.
